# 운영 규칙

## 1. 기준 저장소와 배포

- 프로젝트 산출물 카탈로그, JSON, CSV, 화면 소스의 기준 저장소는 `hudsonkim77/rfp-deliverable-matrix`이다.
- 기준 브랜치는 `main`이며, `main` 반영 결과는 Vercel 배포본으로 확인한다.
- `my-wiki-02` 등 로컬 작업공간은 분석·검증용 임시 공간이며 기준 데이터 저장소가 아니다.

## 2. 원본 폴더

- `F:\고향사랑e음`은 원본 참조 경로로만 사용하며 수정하지 않는다.
- 폴더 구조를 카탈로그화할 때에는 산출물·관리기록의 대표 폴더만 관리한다.
- 제출본, 수행사별 작업본, 백업/OLD, 월별 보관, 복사본, 사진·발표자료·참고자료 폴더는 대표 산출물 목록에서 제외한다.

## 3. 검증 산출물 파일화

- 분석·추출·변환 결과는 반드시 기준 저장소의 로컬 작업본 안에 실제 파일로 생성한다.
- 채팅의 요약, 표, 건수 보고만으로는 검증을 완료한 것으로 보지 않는다.
- 검증 요청 시 사용자가 직접 열 수 있는 파일 경로와 파일명을 보고한다.
- 사용자가 해당 파일의 내용을 확인하고 승인한 후에만 전체 적용 및 배포 단계로 진행한다.
- 임시 검증 파일과 기준 반영 파일을 구분하고, 기준 반영 전에는 GitHub에 업로드하지 않는다.

## 4. 변경 절차

1. 일부 범위를 기준 저장소의 로컬 작업본에 파일로 생성한다.
2. 생성 파일의 경로, 적용 기준, 검증 결과를 보고한다.
3. 사용자가 파일을 직접 확인하고 승인한다.
4. 승인 후 전체 반영한다.
5. `main` 반영 및 Vercel 배포를 확인한다.

사용자가 전체 적용 또는 즉시 배포를 명시한 경우에만 해당 절차를 생략할 수 있다.

## 5. 보안과 기록

- 내부 폴더명, 협력사명, 원본 자료 등 공개 민감성 여부를 확인한 뒤 저장소에 반영한다.
- 기준 데이터의 변경은 파일 경로, 변경 사유, 검증 결과를 함께 기록한다.
